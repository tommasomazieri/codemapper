# codemapper API — Packages

> **Status: stub — not yet documented.**

## Planned endpoints

- `GET /packages` — Lists all installed Python packages (name + version) and which indexed files import each one

Full documentation coming in the next iteration.
